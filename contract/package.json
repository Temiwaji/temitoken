{
  "name": "temitoken-contract",
  "version": "1.0.0",
  "private": true,
  "description": "TemiToken (TMT) - fixed supply ERC20 built with Hardhat + OpenZeppelin",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.6.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "6.1.2",
    "dotenv": "^16.4.5",
    "hardhat": "2.29.0"
  }
}
